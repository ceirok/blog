{% extends 'base.html.twig' %}

{% block body %}
    {% if ufer %}
        <h5>Congratulations!</h5>
        <p>Your account is confirmed! You can now sign up in <a href="{{ path('security_login') }}">here</a></p>
    {% endif %}
{% endblock %}